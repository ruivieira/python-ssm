from distutils.core import setup
setup(
    name = 'ssm',
    packages = ['ssm'], # this must be the same as the name above
    version = '0.0.1',
    description = 'Python library for state-space models',
    author = 'Rui Vieira',
    author_email = 'ruidevieira@googlemail.com',
    url = 'https://github.com/ruivieira/python-ssm', # use the URL to the github repo
    download_url = 'https://github.com/ruivieira/python-ssm/archive/0.0.1.tar.gz', # I'll explain this in a second
    keywords = ['ssm', 'statistics', 'scientific', 'state-space', 'models'], # arbitrary keywords
    classifiers = [],
)